module game {
    export class PCRouletteUI1 extends RouletteBaseUI {

        public constructor(data) {
            super(data);
        }

        // ---------------------------------------- 皮肤组件 ----------------------------------------

        // ---------------------------------------- 变量声明 ----------------------------------------

        // ---------------------------------------- 初始化 ----------------------------------------

        /** 注册事件监听器 */
        public initListener(mediator: RouletteMediator): void {
            super.initListener(mediator);
        }

        public initSetting(): void {
            super.initSetting();
        }

        // ---------------------------------------- 操作筹码 ----------------------------------------

        // ---------------------------------------- dispos ----------------------------------------

        public dispose(): void {
            super.dispose();
        }

    }
}